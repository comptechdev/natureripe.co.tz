<div class="{{ $show == false ? 'hidden md:block' : '' }} md:flex items-center space-x-1">
    <!-- Simplicity is the essence of happiness. - Cedric Bledsoe -->
    <a href="{{ $href }}" class="px-2 text-red-600 hover:border-b-2 border-red-600 font-semibold">{{ $title }}</a>
</div>